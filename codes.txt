<body  ng-app="exampleDialog" ng-controller="MainCtrl">
<script type="text/ng-template" id="firstDialogId">
<div class="ngdialog-message">
<h3>ngDialog template</h3>
<p ng-show="theme">Test content for <code>{{theme}}</code></p>
<p ng-show="ngDialogData.foo">Data passed through directive: <code>{{ngDialogData.foo}}</code></p>
</div>
<div class="ngdialog-buttons">
<button type="button" class="ngdialog-button ngdialog-button-primary" ng-click="openSecond()">Open next</button>
</div>
</script>
</body>

MAKES BUTTON POP UP WHEN YOU STICK IN INDEX.
BREAKS PROGRAM THOUGH.







   <cfif isdefined("form.fileData")> 
      <cfscript>  
      	writeOutput("{{x[0]}}");
            hello = FileUpload("C:\ColdFusion10\cfusion\wwwroot\chilipos\img");
            //f = new proxy.setProductList();
            //f.setIcon('{{productList[$index]}}', hello.CLIENTFILE);
	
           // proxy.SetProductList.setIcon(item['productlistid'],"/img/"&hello.CLIENTFILE);
           // writeDump(proxy);
      </cfscript> 
      <cfset varx =#hello.CLIENTFILE#> 
<cfelse> 
      <cfform name="myUpload" enctype="multipart/form-data"> 
            <cfinput type="file" name="fileData"><br> 
            	
            <cfinput ng-click="changeIcon('','hey')" type="submit" name="submit"> 
      </cfform> 
</cfif>

